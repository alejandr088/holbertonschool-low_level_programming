#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
/* more headers goes there */
int main(void)
{
	char c = 'a';

	for (c = 'a'; c <= 'z'; c++);

	putchar(c);
	putchar('\n');

	return (0);
}


