#include "main.h"

/**
 * mul - check code
 * @a: single character input
 * @b: single character input
 * Return: 1 si esta en minusculas, de lo contrario 0
 */
int mul(int a, int b)
{
int mul = a * b;
return (mul);
}
