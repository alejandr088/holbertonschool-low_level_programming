#include "main.h"
/**
 * print_square - check code
 * @size: single character
 */
void print_square(int size)
{
int a;

if (size > 0)
{
for (a = size; a >= size; a++)
{
putchar('#');
}
}
else
{
putchar('\n');
}
}
