#include "main.h"

/**
 * print_most_numbers - check code
 *
 * Return: Always 0
 */
void print_most_numbers(void)
{
int num;
while ((num = 0 && num <= 9) && (num != 2 || num != 4))
{
putchar(num);
num++;
}
putchar('\n');
}
