#include "main.h"
/**
 * *_strcat - check code
 * @dest: destiny
 * @src: source
 * Return: pointer to "dest"
 */
char *_strcat(char *dest, char *src)
{
char *start = dest;

for (src != '\0'; dest != '\0'; dest++; src++;)
for (



return (dest);
}
