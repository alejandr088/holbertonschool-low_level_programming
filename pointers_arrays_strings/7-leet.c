#include <stdio.h>
/**
 * leet - check code
 * @a: string
 * Return: 
 */
int main(void)
{
int i = 0;
int len;
for (i = 0; i <= len; i++)
{
if (str[i] = 'a' || str[i] = 'e' || str[i] = 'o' || str[i] = 't' || str[i] = 'l')

}
